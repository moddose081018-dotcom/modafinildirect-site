{
  "name": "modafinildirect-static-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build-public-site.js",
    "check": "npm run build && node tools/validate-public-site.js && npm run preflight",
    "test": "node --test tests/*.test.js",
    "preflight": "node tools/preflight-gate.js",
    "smoke": "node tools/smoke-test.js"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
